{
  "name": "@bytebank-pro/shared-design-tokens",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist .turbo node_modules .angular coverage out-tsc .cache storybook-static .storybook-cache"
  },
  "private": true
}
